import React from "react";

export type CityType = {
  id: string;
  name: string;
  country: string;
  costs: number;
  image: string;
};

const Citylist = () => {
  return <div>Citylist</div>;
};

export default Citylist;
