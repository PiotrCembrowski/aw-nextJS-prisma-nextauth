generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Country {
  country_id String @id @default(cuid())
  name       String
  costs      Int
  continent  String
  cities     City[]
}

model City {
  city_id              String   @id @default(cuid())
  name                 String
  costs                Int
  countries_country_id String?
  country              Country? @relation(fields: [countries_country_id], references: [country_id])
  posts                Post[]
}

model Post {
  id             String  @id @default(cuid())
  title          String
  content        String?
  published      Boolean @default(false)
  authorId       String?
  cities_city_id String?
  author         User?   @relation(fields: [authorId], references: [id])
  city           City?   @relation(fields: [cities_city_id], references: [city_id])
}

model User {
  id             String   @id @default(cuid())
  email          String   @unique
  password       String?
  userName       String?  
  createdAt      DateTime @default(now()) @map("created_at")
  updatedAt      DateTime @updatedAt @map("updated_at")
  img            String?
  authProviderId String?
  posts          Post[]
}
